{
  "name": "pasteboard",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "check-types": "tsc --noEmit --project tsconfig.json",
    "build": "npm run check-types & esbuild src/index.ts --tsconfig=tsconfig.json --platform=node --bundle --outfile=build/server.js",
    "start": "npm run build && node build/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "graphql": "^16.3.0",
    "nunjucks": "^3.2.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/nunjucks": "^3.2.1",
    "esbuild": "^0.14.25",
    "typescript": "^4.6.2"
  }
}
