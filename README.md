# Getting started

Start up the server:
```
cd server
npm install
npm run dev
```

Start up the client:
```
cd client
npm install
npm run dev
```

Open up `http://localhost:5173` and you should see the an empty boards page.

# Docker builds

```
docker build -t pb:x.y.z .
```